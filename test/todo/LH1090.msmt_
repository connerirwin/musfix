; Sorts
(declare-sort Either 2)

; Constants




; Uninterpreted Functions


; Qualifiers


; Well-formedness Constraints


;  Constraints
(constraint (forall ((punk (Either Int Int))(junk (Either Bool Int))(escobar Int)) (=> (and (= junk (left escobar)) True) (= punk (left escobar)))))

