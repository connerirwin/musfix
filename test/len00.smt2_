(qualif ListZ ((v (Set_t @0))) (>= (len v) 0))

(define-fun len (@0 @1) Int)

(wf (Set_t $k0) ((v (Set_t (Tuple Int @a)))))

(constraint
  (forall ((v (Set_t (Tuple Int @a))))
    (=> (>= (len v) 0)
        ((Set_t $k0)))))

(constraint
  (forall ((v (Set_t (Tuple Int @a))))
    (=> ((Set_t $k0))
        (>= (len v) 0))))
