; Sorts
(declare-sort Vec 1)

; Constants




; Uninterpreted Functions
(declare-fun len ((Vec @a0)) Int)

; Qualifiers


; Well-formedness Constraints


;  Constraints
(constraint (forall () (=> (= (len (VCons 1 (VCons 2 (VCons 3 VNil)))) (+ 1 (+ 1 (+ 1 0)))) (= (len (VCons 1 (VCons 2 (VCons 3 VNil)))) 3))))

