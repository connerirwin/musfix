; Uninterpreted Sorts
(declare-sort List 1)


; Constants


; Distinct Constants


; Uninterpreted Functions
(declare-fun foo (@a0) Int)
(declare-fun bar (Bob) Int)


; Qualifiers


; Well-formedness Constraints


; Horn Constraints
(constraint (forall ((v1 Int)) (=> (= (apply foo z) unsupported expression [ECoerc FInt FInt (ECon (I 22))]) (= (apply foo z) 22))))
