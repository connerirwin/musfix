(qualif ListZ ((v (Set @0))) (>= (len v) 0))

(define-fun len (@0 @1) Int)

(wf (Set $k0) ((v (Set (Tuple Int @a)))))

(constraint
  (forall ((v (Set (Tuple Int @a))))
    (=> (>= (len v) 0)
        ((Set $k0)))))

(constraint
  (forall ((v (Set (Tuple Int @a))))
    (=> ((Set $k0))
        (>= (len v) 0))))
