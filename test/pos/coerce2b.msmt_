; Sorts
(declare-sort List 1)
(declare-sort Obj_b 0)

; Constants




; Uninterpreted Functions
(declare-fun len ((@t0 @a1)) Int)

; Qualifiers


; Well-formedness Constraints


;  Constraints
(constraint (forall ((pig Obj_b)) (=> (= (len pig) 10) (> (len pig) 5))))

